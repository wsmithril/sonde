[package]
name       = "sonde-usb"
version    = "0.1.0"
edition    = "2024"
authors    = ["wsmithril <wsmithril@gmail.com>"]
license    = "MIT"
repository = "https://github.com/wsmithril/sonde"
description = "USB-CDC-logging build of Sonde: decoded ASCII console over USB, QSPI name tables, provisioning"

[[bin]]
name = "sonde-usb"
path = "src/main.rs"

[features]
# Propagates to the shared library.
resolve-identities = ["sonde-common/resolve-identities"]

# Versions and features are defined once in the workspace root (see
# `[workspace.dependencies]` there).
[dependencies]
sonde-common = { path = "../common" }

embassy-executor = { workspace = true }
embassy-time     = { workspace = true }
embassy-nrf      = { workspace = true }
embassy-sync     = { workspace = true }
embassy-usb      = { workspace = true }
embassy-futures  = { workspace = true }
static_cell      = { workspace = true }
heapless         = { workspace = true }
embedded-storage = { workspace = true }
defmt            = { workspace = true }
defmt-rtt        = { workspace = true }
cortex-m         = { workspace = true }
cortex-m-rt      = { workspace = true }
